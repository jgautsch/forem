<% if can?(:read, category) %>
  <div id='category_<%= category.id %>' class='category col-md-12'>
    <h2><%= link_to forem_emojify(category.name), [forem, category] %></h2>
    <%= render "/forem/forums/listing", :category => category %>
  </div>
<% end %>
